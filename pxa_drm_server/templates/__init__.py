"""``pxa-drm-server init`` 이 복사해 주는 시작 템플릿.

- ``domain_core.py`` : 개발자가 구현할 세 함수 (동작 확인용 샘플 포함)
- ``config.yaml``    : 설정 파일 기본형

저장소 루트의 ``domain_core.py`` / ``config/config.yaml`` 과 동일한 내용이며,
tests/test_packaging.py 가 두 쪽이 어긋나지 않는지 검사한다.
"""
